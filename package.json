{
  "name": "orchard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:server": "pnpm --filter @orchard/server dev",
    "dev:web": "pnpm --filter @orchard/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "openai": "^6.17.0"
  }
}
